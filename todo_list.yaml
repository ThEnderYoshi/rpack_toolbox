# Things I still need to add (or would be nice to add) to this program
#
# Items marked with '#!' should be finished in the current commit (ideally 🫵YOU
# wouldn't see them..)
#
# Items marked with '#v <...>' should be finished by the time version
# '<...>' releases
app:
- gui integration              #v <gui>
- features to toggle frontends #v <gui>
readme: #v 0.1.0
cli:
- split lib.rs for each tool
- allow Args to set verbosity #v 0.1.0
generate:
  gui: #v <gui>
scan:
  txt:
  json dump: #v 0.1.0
  truncated paths should include the extension: #v 0.1.0
  gui: #v <gui>
jigsaw:
  shared:
  cli:
  gui: #v <gui>
build:
  shared:
  cli:
  gui: #v <gui>
nice to have:
  proper specs for the ref files:
  - flat ref
  - tree ref
  for invalid file names, have the scanner try to find similar names in the ref:
  optional suggestion list for scan:
  allow black/whitelisting asset kinds in scan:
