use super::*;

// Runs all of the built-in Jigsaw configs (at '/jigsaw') and compares the
// results against their pre-generated counterparts (at '/tests/jigsaw_cfgs')
#[tokio::test]
async fn test_built_in_cfgs() {}
